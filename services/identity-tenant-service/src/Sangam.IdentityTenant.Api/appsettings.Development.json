{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "Jwt": {
    "//": "Local development only. Real environments supply Jwt__SigningKey from a secret store, never from a checked-in file.",
    "SigningKey": "local-development-signing-key-do-not-use-in-production"
  },
  "Bootstrap": {
    "//": "Creates the first Super Admin on an empty database. Never reuse these values outside local development.",
    "SuperAdminIdentifier": "superadmin@samaajconnect.local",
    "SuperAdminPassword": "change-me-immediately",
    "SuperAdminName": "Platform Super Admin"
  }
}
