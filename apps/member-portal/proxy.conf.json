{
  "//": "Local development only. Sends API calls to the gateway, which runs on its own port. The platform is single-domain and the tenant travels in the token, so nothing about the Samaaj is set here.",
  "/v1": {
    "target": "http://localhost:8080",
    "secure": false,
    "changeOrigin": false
  }
}
